spring:
  profiles:
    group:
      "local": "local, key"
      "dev": "dev, key"
      "test": "test, key"
    active: local
  servlet:
    multipart:
      #file upload size
      max-file-size: 3MB